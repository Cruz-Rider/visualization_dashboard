# Virtualization Dashboard

## Installation

### Prerequisites

- Python (>=3.6)
- Django (>=3.0)
- Node.js (>=12.0)
- React (>=16.8)

### Backend Setup

1. Navigate to the backend directory:

    ```bash
    cd virtualization-dashboard/
    ```

3. Install Python dependencies:

    ```bash
    pip install -r requirements.txt
    ```

4. Apply database migrations:

    ```bash
    python manage.py migrate
    ```

5. Run the Django development server:

    ```bash
    python manage.py runserver
    ```

### Frontend Setup

1. Navigate to the frontend directory:

    ```bash
    cd virtualization-dashboard/frontend
    ```

2. Install Node.js dependencies:

    ```bash
    npm install
    ```

3. Start the React development server:

    ```bash
    npm start
    ```

4. Open your browser and visit [http://localhost:3000](http://localhost:3000) to access the Virtualization Dashboard.

## Contact

For any inquiries or issues, please contact [1999shishirshukla@gmail.com].

Happy virtualizing!
